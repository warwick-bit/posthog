{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
        "force_refresh": {
            "default": false,
            "description": "When true, skip the cache and rebuild the profile from authoritative sources before responding. Use after seeding events, importing data, or any other change the caller knows just landed but hasn't surfaced through natural cache expiry yet. Concurrent forced rebuilds are still serialized by the team-keyed advisory lock — at most one extra `build_inventory` per simultaneous request.",
            "type": "boolean"
        }
    },
    "type": "object"
}
